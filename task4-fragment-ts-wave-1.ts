// ВНИМАНИЕ: в этом фрагменте есть несколько ошибок и плохих практик.
// Кандидату нужно:
// 1) Найти и описать проблемы.
// 2) Предложить, как переписать код лучше.

// "Предложить исправленный вариант фрагмента кода (можно текстом или патчем)"
// тут я напишу текстом.. за последние недели очень большая загрузка... Извините :(

// Проблемы которые я увидел:
//   - гонка данных
//         eсли быстро печатать слово, отправится n запросов. Запрос на букву n может прийти после запроса на слово n и перезаписать старыми данными

//         Решение:
//             использовать AbortController для отмены предыдущего запроса или игнорировать результаты старых запросов по ID/таймстампу

//   - Отсутствие обработки ошибок в fetch
//         fetch не выбрасывает ошибку (throw), если сервер ответил 404 или 500. попытка распарсить ошибку как JSON и упадет
//         Также нет блока try/catch

//         Решение:
//             проверять res.ok после fetch и оборачивать вызов в try/catch/finally

//     - "Вечный" индикатор загрузки
//         если fetch упадет с ошибкой, в кода нет сброса в false

//         Решение:
//             сбрасывать isLoading = false в блоке finally

//     - Глобальный стейт и мутация
//         массив devices лежит в глобальной области и перезаписывается функцией. Это делает код непредсказуемым и сложным для тестов

//         Решение:
//             функция должна возвращать данные, а не менять глобальные переменные. Состояние должно управляться локально или через стейт-менеджер

//     - Нагрузка на сеть
//         запрос улетает на каждый нажатый символ. Это спам сервера и тормозит интерфейс

//         Решение:
//             добавить задержку 300-500 мс перед отправкой запроса

//     - Плохая логика фильтрации
//         поиск через indexOf чувствителен к регистру и лишним пробелам

//         Решение:
//             использовать нижний регистр и обрезать пробелы .toLowerCase() и .trim()

//     - Ошибки типизации
//         Device = { ... } — это синтаксическая ошибка в ts

//         Решение:
//             нормально объявлять типы interface Device

type Device = {
  id: number;
  hostname: string;
  ip: string;
};

// Имитация запроса к API
async function fetchDevices(): Promise<Device[]> {
  // Потенциальная проблема: игнорируются ошибки сети/HTTP-код
  const res = await fetch("/api/devices");
  return (await res.json()) as Device[];
}

// Глобальное состояние (антипаттерн для большинства приложений)
let devices: Device[] = [];
let isLoading = false;

export async function loadAndFilterDevices(search: string) {
  isLoading = true;

  // Потенциальная проблема: нет try/catch, при ошибке состояние "подвиснет"
  const data = await fetchDevices();

  // Потенциальная проблема: мутируем общий массив из разных мест
  devices = data;

  // Потенциальная проблема: сравнение без нормализации регистра и trim
  const filtered = devices.filter((d) => d.hostname.indexOf(search) >= 0);

  // "забываем" сбросить isLoading в случае ошибок выше
  isLoading = false;

  // Потенциальная проблема: функция имеет побочные эффекты и возвращает разные типы
  // (в реальном коде сюда часто добавляют ещё логику, что делает её трудной для тестирования)
  return filtered;
}

// Пример использования (упрощённо)
async function example() {
  const searchInput: HTMLInputElement | null =
    document.querySelector("#search");
  if (searchInput) {
    // Потенциальная проблема: нет debounce, каждый ввод символа может бить по API
    searchInput.oninput = async () => {
      const list = await loadAndFilterDevices(searchInput.value);
      console.log("Devices:", list);
    };
  }
}

example();
